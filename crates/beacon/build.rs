fn main() {
    // Intentionally empty.
    //
    // Frontend build + embed is handled by the `beacon-frontend-embed` crate so that
    // backend changes don't invalidate the embedded-asset compilation unit.
}
