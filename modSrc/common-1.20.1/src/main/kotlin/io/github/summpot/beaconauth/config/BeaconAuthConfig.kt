package io.github.summpot.beaconauth.config

// Moved to modSrc/common/src/main/kotlin/io/github/summpot/beaconauth/config/BeaconAuthConfig.kt
//
// This file intentionally contains no declarations. 1.20.1 uses ForgeConfigSpec-based configs,
// which are now registered inside the loader modules (fabric-1.20.1 / forge-1.20.1) and applied
// to the shared config storage via BeaconAuthConfig.apply(...).
