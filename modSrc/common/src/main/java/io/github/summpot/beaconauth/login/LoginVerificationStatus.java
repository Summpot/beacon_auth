package io.github.summpot.beaconauth.login;

/**
 * Intentionally empty.
 *
 * The shared type {@code LoginVerificationStatus} is implemented in Kotlin
 * (see common/src/main/kotlin) so it is available during Kotlin compilation.
 */
