package io.github.summpot.beaconauth.login;

/**
 * Intentionally empty.
 *
 * The version-specific type {@code LoginQueryType} is implemented in Kotlin
 * (see src/versioned/kotlin) so it is available during Kotlin compilation.
 */
